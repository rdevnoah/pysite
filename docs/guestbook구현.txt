1. model
2. /guestbook/deleteform/10
    urls.py
    path('helloworld/hellow2/<init:id>', helloworld_views.html
    def hello2(request, id=0):
    return HttpResponse(f'id:{id}')

3. orm method
    index(list) -> ok
    add         -> ok
    delete
    1. repository에서 객체를 영속화
        guestbook = Guestbook.objects.filter(id=20).filter(password='1234')
        guestbook.delete()

[구현 안해도 되는 것]
list.html 템플릿
1. 메세지 index 접는 것 하지말 것(django template arith operation)
2. 개행문자 <br> replace 하지말 것(django template arith operation)